# 开发环境配置
VITE_APP_TITLE=博客管理系统(开发)

# API配置（只用于管理后台）
VITE_API_BASE_URL=/api
VITE_USE_MOCK=false

# 调试配置
VITE_APP_DEBUG=true

# 前台博客API配置（注释说明，前台博客直接使用/blog路径，不使用环境变量）
# 前台博客API通过src/utils/blogRequest.js独立配置，不依赖此处环境变量 