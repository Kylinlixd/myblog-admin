# 访问画像与仪表盘实施计划

目标：接入 360 属地缓存及历史刷新，统一四级风险和白名单优先级，支持分页前组合筛选，完成仪表盘下方五个数据卡片。

设计：保留顶部区域和既有卡片样式。下方发布/主题两卡，随后三列访问、热门、安全卡。ECharts 按需加载、浅色、tooltip、容器 resize 与卸载清理。所有统计来自 Django API。

1. 后端 `apps/access_log`：先测 Unicode/运营商解析、缓存/失败回退、历史去重刷新、白名单 CIDR/过期、四级筛选和分页总数；实现在线查询、缓存模型及命令、服务端组合过滤。
2. 后端 `apps/dashboard`：先测完整七个自然日、仅已发布内容、阅读量排序、文章 TOP5、日志 PV/UV/会话估算和七日风险；实现统计 API。
3. 前端 `src/views/dashboard`：扩展映射和图表配置测试；替换旧 SVG，增加三列卡片、阅读进度条与有效路由。
4. 前端 `src/views/accessLogs`：风险四级、网络/地区输入、后端汇总数字与白名单展示；验证参数与重置。
5. 执行 Django 测试、Jest、生产构建和浏览器桌面/窄屏检查。历史刷新需对实际业务数据库运行命令并核对成功/失败数量。

口径：今天及前六天按 Asia/Shanghai 自然日。PV 为成功 GET 公开文章详情请求；UV 为 IP+UA 去重；30 分钟无访问拆分会话，单次阅读会话占比为估算跳出率。主题权重及热门文章为已发布文章累计 view_count。风险评分基于最近七日请求，白名单有效时覆盖为低风险，保留行为指标。

## 交付记录（2026-09-07）

- 前端已发布至 `/var/www/myblog-admin/releases/20260907-access-dashboard`，current 已切换；后端位于 `/opt/blog_li`，迁移 `access_log.0004_ipgeocache` 已执行。
- 360 POST 需携带 `User-Agent: Mozilla/5.0` 和 `Referer: http://ip.360.cn/`；缺少请求头时实测返回 403。JSON 直接解码 Unicode，按制表符分离运营商，失败不覆盖既有成功属地。
- 历史刷新初轮 217 个公网 IP 中 216 个成功；重试补齐 1 个失败项及 4 个新公网 IP，共 5 个成功、0 个失败。
- `blog-ip-geo.timer` 已启用，按上次完成后 10 分钟调度。缓存命中保留 30 天，失败暂存 10 分钟，单个画像可即时查询。
- 后端全量 119 项通过（1 项已有跳过）；前端仪表盘与访问筛选专项 13 项通过。此前前端全量排除未跟踪副本 `BlogHomeExperience.spec 2.js` 后 319 项通过；该副本 12 项旧首页断言失败，保留原文件。
- Vite 生产构建通过。桌面五卡布局、390px 窄屏无横向溢出、两图渲染及主图 tooltip 双数值已实测。真实数据库验证风险/网络/地区过滤、白名单排除和七天完整序列；统计接口约 213ms。
- 统计口径明确标注：日志只能提供文章详情 PV、IP＋浏览器 UV，以及 30 分钟会话估算跳出率，并非完整前端行为埋点。
- 本次提交仅包含访问画像、仪表盘及配套测试和文档；博客首页及其他工作区改动由对应任务独立处理。
- 最终覆盖核对：235 个历史独立 IP，221 个公网全部解析成功，14 个非公网本地识别，未解析公网 0 个。定时任务 Result=success、timer active；Nginx 和 blog-li active，仪表盘 HTTP 200。
