# 2026-07-27 前端交付记录

## 节点 1：体验基线与方案确认

- 状态：完成
- 方向：夜蓝数字花园；阅读体验与个人品牌并重
- 范围：桌面、平板、移动浏览器响应式适配
- 导航：均衡悬浮式导航，移动端折叠菜单

## 节点 2：公开站点重构

- 状态：完成
- 首页改为 AIDA 信息结构，首屏使用最新文章封面并提供无内容回落状态
- 增加文章 Bento 编排、重点文章层叠区、分类折叠区与标签流
- 导航、按钮、卡片和路由增加点击、加载与失败反馈
- 文章详情 Markdown 输出和评论输入增加 DOMPurify 清理

## 节点 3：管理端发布体验

- 状态：完成
- 编辑器改为主写作列与 320px 设置栏，窄屏自动回落为单列
- 增加本地草稿恢复、离开页面保护、保存中禁用与成功后清理
- 关闭公开注册链接与路由，登录页明确为管理员入口

## 节点 4：Ponytail 代码清理

- 状态：完成
- 删除旧注册页、调试页、重复编辑器、重复动态表单与未引用工具
- 删除未使用依赖与无效手工分包配置
- 清理生产代码中的浏览器调试输出，避免记录密码、正文和上传响应
- 本阶段提交净删除超过 3,600 行遗留代码

## 节点 5：验证

- 状态：完成
- 自动化测试：30 个测试套件、63 个测试全部通过
- 生产构建：Vite 构建通过
- 生产依赖审计：0 个已知漏洞
- 实机检查：1440×1000 桌面宽度与 375×844 移动宽度通过
- 响应式检查：无横向溢出；移动导航可展开；接口不可用时显示可重试状态

## 节点 6：Git 与生产发布

- 状态：完成
- 目标仓库：Kylinlixd/myblog-admin
- Git 主分支版本：`0ecc0e4`
- 生产版本目录：`/var/www/myblog-admin/releases/0ecc0e4`
- 当前版本链接：`/var/www/myblog-admin/current`
- 生产域名：https://leexd.top
- 验收：HTTP 自动跳转 HTTPS；桌面与移动端真实数据加载、移动导航、控制台状态均通过
- 发布前备份：`/root/backups/kylin-blog-20260727T073026Z`

## 节点 7：首页文案与管理端列表统一

- 状态：完成
- 首页首屏文案更新为“探索技术的无限可能”并保持桌面、移动端两行排版
- 内容、分类、标签、评论、文件五个列表统一标题、副标题、筛选、操作区、表格卡片与分页节奏
- 分类、标签和内容的主操作统一放入页面标题区，批量操作保留在工具栏
- 移除侧栏中生产环境不准确的“本地开发 / API 已代理到 8000”提示
- 复用既有 `PageHeader` 与 `admin-workspace.scss`，未增加前端依赖
- 实现提交：`4fdfe07`
- 入口挂载健康标记提交：`4cd825a`

## 节点 8：Ponytail 清理结果

- 状态：完成
- 删除分类、标签、内容、文件页面中被共享工作区样式覆盖的重复 CSS
- 删除内容页重复的“新建动态”操作与未使用处理函数
- 核心页面与组件代码共新增 171 行、删除 602 行；含测试和设计文档后的实现提交净删除 279 行

## 节点 9：服务器清理与管理员机制核验

- 状态：完成
- 已删除发布备份：`/root/backups/kylin-blog-20260727T073026Z`（删除前 1.3 MB）
- 已删除后端回滚副本：`/opt/blog_li-rollback-20260727T073026Z`（删除前 82 MB）
- 已删除本次不完整传输产生的缺陷前端发布目录：`/var/www/myblog-admin/releases/4fdfe07`
- 生产数据库现有 2 个普通用户，`is_staff` 与 `is_superuser` 数量均为 0
- 公共注册已关闭；注册接口只允许 `IsAdminUser` 调用
- 正式管理员应在服务器通过 `/opt/blog_li/.venv/bin/python /opt/blog_li/manage.py createsuperuser` 交互创建，密码由 Django 哈希保存，不存储明文

## 节点 10：最终验证与生产发布

- 状态：完成
- 自动化测试：30 个测试套件、66 个测试全部通过
- 生产构建：Vite 构建通过
- 浏览器验收：1440×1000 与 375×844 均显示新文案，无横向溢出，移动端主按钮高度 48px
- 线上挂载标记：`data-app-ready="true"`
- 生产版本目录：`/var/www/myblog-admin/releases/4cd825a`
- 当前版本链接：`/var/www/myblog-admin/current`
- 生产域名：https://leexd.top
- 验收：HTTPS 200；HTTP 301 跳转 HTTPS；入口与懒加载资源 MIME 正确；浏览器控制台无错误
- 服务状态：nginx、blog-li、MySQL 均为 `active`

## 节点 11：夜蓝数字花园完整视觉重构

- 状态：完成
- 首页 Hero 更新为“探索技术 / 无限可能”两行电影感排版，桌面最大字号与移动端行数均受控
- 增加固定星雾、网格、光团与噪点环境层，并对减少动效设置提供完整降级
- 增加 GSAP 滚动文字擦除揭示、精选图片缩放淡出与内容卡片滚动叙事
- 保留并强化 12 列 Bento、横向分类手风琴与标签跑马灯，新增手动“创作宣言”轮播
- 动态、分类、标签、搜索、关于与详情页统一接入夜蓝主题、章节页头和暗色卡片语言
- 删除公共动态页测试数据按钮、模拟数据回退和重复雪花背景
- 实现提交：`5792d77`、`734282a`、`b0d6669`

## 节点 12：后台列表与仪表盘完整优化

- 状态：完成
- 动态、分类、标签、文件、评论五个列表统一为 30px 行内动作按钮
- 主要操作使用蓝色弱底，普通操作使用中性弱底，危险操作使用红色弱底；原确认与权限逻辑保持不变
- 仪表盘新增品牌概览带、真实内容实体总量、4 项 KPI 比例、12 列趋势区、内容构成和快捷动作
- 所有比例均由 `/api/stats/` 现有数据派生，不展示不存在的同比或增长率
- 实现提交：`15f682d`、`2954861`

## 节点 13：Ponytail 冗余清理与验证

- 状态：完成
- 删除 4 个从未导入的旧样式文件、7 组无运行时消费者的旧组件及孤立测试
- 删除废弃模拟数据开关与动态页死函数，未增加依赖
- 本轮完整实现新增 742 行、删除 3,537 行，净减少 2,795 行
- 自动化测试：32 个测试套件、77 个测试全部通过
- 生产构建：Vite 构建通过
- 生产依赖审计：`npm audit --omit=dev --audit-level=high` 为 0 个已知漏洞
- 浏览器核验：1200px 桌面与 390×844 移动视口通过，无横向溢出；移动导航正确折叠
- 清理提交：`240a928`

## 节点 14：Git 与生产发布终验

- 状态：完成
- GitHub 主分支实现版本：`8f6e653`
- 生产版本目录：`/var/www/myblog-admin/releases/8f6e653`
- 当前版本链接：`/var/www/myblog-admin/current`
- 构建文件：174 个；传输压缩包 SHA-256 为 `5ae8d01200f1adc194a2e01eeceabe4d986f44cde860e87d93ea897f4fe74f23`
- 生产域名：https://leexd.top
- 外网验收：HTTPS 200；HTTP 301 跳转 HTTPS；最近内容接口 200
- 浏览器终验：桌面与 390×844 移动视口均加载 `data-app-ready="true"`，Hero、动态环境层、滚动文字和统一内页主题生效，无横向溢出
- 服务状态：nginx、blog-li、MySQL 均为 `active`
- 本次未创建服务器备份；本地与远端传输临时压缩包均已删除

## 节点 15：项目文件与依赖清理

- 状态：完成
- 删除 5 个无消费者或重复的前端文件：废弃 Service Worker、失效 API 聚合入口、未使用环境工具、编辑器专属配置与重复头像资源
- 默认头像统一复用 `public/default-avatar.png`；删除已废弃的 mock、博客 API 环境变量说明
- 删除 6 个无直接消费者的依赖及失效 lint 脚本，锁文件净减少约 1,000 行
- 自动化测试：32 个测试套件、78 个测试全部通过
- 生产构建：Vite 构建通过
- 生产依赖审计：`npm audit --omit=dev --audit-level=high` 为 0 个已知漏洞
- 线上运行代码未改变，因此无需重复发布生产服务器

## 节点 16：首页感官重设计与历史文件复核

- 状态：完成
- Git 历史审查以 2025-07-27 为界；Django migrations、空 `__init__.py`、ASGI/WSGI、入口文件及仍有运行时引用的前端文件全部保留
- 删除未被任何模板使用的 Tailwind 入口、Tailwind 配置与手写 UUID 工具，移除 `tailwindcss`、`tailwindcss-animate` 两个依赖
- 清理提交新增 26 行、删除 798 行，净减少 772 行；请求 ID 改用浏览器原生 `crypto.randomUUID()`
- 首页改为 7/5 非对称夜蓝舞台，“探索技术 / 无限可能”保持两行并重新校正行高、说明和按钮节奏
- 第二行加入精选媒体入口；精选窗口增加指针透视、边缘折射和动态聚光，触屏与减少动效设置自动降级
- GSAP 增加桌面文章导读固定、理念文字 scrub 和内容卡片层叠；删除无意义的英文编号标签
- 修复高屏 `100dvh` 空场：首页高度改为 `clamp(760px, 86dvh, 980px)`，1080×1500 下下一章节从 1042px 开始进入视口
- 浏览器核验：1080×1500 与 390×844 均保持两行标题且无横向溢出；移动端按钮、精选窗口无碰撞；控制台无前端错误
- 本地视觉核验覆盖接口不可用时的正式空状态；真实内容继续由生产 API 提供
- 自动化测试：32 个测试套件、81 个测试全部通过
- 生产构建：Vite 构建通过；`git diff --check` 通过
- 实现提交：`f94cc09`、`297ec29`、`3fe85ea`、`31ba219`
- Git `main` 已推送至 `57c19ba`；远端分支与本地一致
- GitHub `Deploy` 工作流处于 `disabled_manually`，本次沿用不可变目录方案直接发布，不更改该仓库设置
- 生产版本目录：`/var/www/myblog-admin/releases/57c19ba`；`current` 已原子切换到该版本，未创建额外服务器备份
- 线上核验：首页、`/blog`、`/dashboard`、博客 API 与新静态资源均返回 HTTP 200；线上资源指纹为 `index-CLWmQa-M.js`
- 服务状态：nginx、blog-li、MySQL 均为 `active`；Nginx 配置校验通过并已 reload；本地与服务器上传临时包均已删除

## 节点 17：首页第二屏路径宣言

- 状态：完成
- 将“技术不是孤立的答案……”重构为精确三行的沉浸式路径宣言，加入“问题—判断—构建”语义轨道、动态能量进度和夜蓝网格光场
- GSAP 增加路径进度 scrub、信息句与节点渐显、三行文字由模糊低亮到清晰实色的滚动显影；减少动效模式直接展示完整终态
- 桌面端保留递进缩进；移动端取消缩进并使用响应字号与负字距，390×844 下三行宽度约 322 / 322 / 290px，页面宽度 375px，无横向溢出
- 视觉核验：1080×1200 与 390×844 均保持精确三行，第二屏路径完整、第三屏可继续进入；浏览器控制台无错误或警告
- 自动化测试：32 个测试套件、82 个测试全部通过；Vite 生产构建通过；生产依赖审计为 0 个已知漏洞
- 设计、计划与实现提交：`2dfba3f`、`b53f3f5`、`918413d`
- 生产版本目录：`/var/www/myblog-admin/releases/918413d`；`current` 已原子切换到该版本，未创建额外服务器备份
- 服务状态：nginx、blog-li、MySQL 均为 `active`；Nginx 配置校验通过并已 reload；传输临时包均已删除
- 公网核验：首页、`/blog`、`/dashboard`、博客 API 和 `BlogHome-DcZELDjp.js` 均返回 HTTP 200

## 节点 18：后台可靠性、宽屏运营台与阅读可读性

- 状态：实现与验证完成
- 修复动态发布页读取旧 `accessToken` 键导致正常会话被误判过期、提交前跳回登录页的问题
- 媒体上传删除独立 Axios 与重复认证逻辑，统一复用 `api/file.uploadFile`；本批次上传实现由 151 行收敛到 31 行
- 会话初始化仅在确认 401 后清理登录态；临时网络或服务端错误不再主动注销；`auth:expired` 同步清理 Pinia 与持久化会话
- 仪表盘重构为非对称内容运营台：连续指标带、七日发布节奏、真实分类/标签活跃脉络与紧凑快捷操作
- 共享后台工作区宽屏上限由 1480px 调整为 1660px，1920px 显示器获得 1600px 有效内容区，普通桌面与移动端保持响应式降级
- 全局字体统一为 Geist / Segoe UI / PingFang SC / Microsoft YaHei 系统栈；文章正文独立为高对比阅读色、760px 行宽、18px 字号与 1.9 行高
- 评论标签、输入值、占位符、字数计数、昵称、时间与正文重新建立暗色对比层级；详情页评论数量改用评论接口真实总数
- 右下角加载反馈重绘为夜蓝状态浮层，包含状态灯、主副文案、移动安全区和 reduced-motion 回退
- 自动化测试：33 个测试套件、91 个测试全部通过；Vite 生产构建通过；生产依赖审计为 0 个已知漏洞
- 后端验证：Django system check 无问题，34 个测试全部通过
- Chromium/Edge 内核回归：1920×1080、1418×675、390×844 均无横向溢出；实测正文宽 760px，评论与仪表盘宽屏/移动布局正确
- 实现提交：`7922612`、`77c8be2`、`cd8042f`、`132464f`、`31717d2`、`5f27bd3`
