# 关于页联系邮箱对比度优化设计

## 目标

修复关于页底部联系卡片中邮箱文字与深色背景对比不足、难以阅读的问题。

## 方案

- 保持邮箱内容、`mailto:` 链接和卡片布局不变。
- 将邮箱默认颜色改为高对比的暖白色，并保留底部边框作为可点击提示。
- 增加悬停、键盘聚焦和触摸激活状态，使用主题强调色提供反馈。
- 保持移动端换行规则，避免邮箱在窄屏溢出。

## 验证

- 在 `BlogAbout.spec.js` 增加源码回归断言，锁定邮箱颜色和交互状态。
- 运行关于页定向测试、全量测试和生产构建。
