# Vite 资源必须只从当前发布目录读取；缺失的旧 hash 不能回退到 index.html。
location ^~ /assets/ {
    try_files $uri =404;
    access_log off;
    expires 1y;
    add_header Cache-Control "public, max-age=31536000, immutable" always;
}
